<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>


    <style name="trans_dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@drawable/trans_box</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>
    <!--羊皮纸bg-->
    <style name="sp_dialog" parent="@android:style/Theme.Dialog">
        <!-- 更换背景图片实现全透明 -->
        <item name="android:windowBackground">@drawable/radius_bg_dark</item>
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 隐藏标题 -->
        <item name="android:windowNoTitle">true</item>
    </style>
    <!-- 设置dialog弹出，退出动画 -->
    <style name="dialogWindowAnim" parent="android:Animation" mce_bogus="1">
        <item name="android:windowEnterAnimation">@anim/dialog_show_anim</item>
        <item name="android:windowExitAnimation">@anim/dialog_hide_anim</item>
    </style>
</resources>
